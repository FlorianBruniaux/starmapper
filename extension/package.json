{
  "name": "starmapper-extension",
  "version": "1.1.0",
  "private": true,
  "description": "Map the stargazers of any GitHub repo without leaving GitHub",
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "zip": "wxt zip",
    "typecheck": "wxt prepare && tsc --noEmit",
    "icons": "node scripts/generate-icons.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.42",
    "typescript": "^5.8.3",
    "wxt": "^0.20.0"
  }
}
