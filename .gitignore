# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Visual brainstorming sessions
.superpowers/

# dependencies
/node_modules
mcp/node_modules
mcp/dist
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
*.key
*.cert
*.p12
*.pfx

# IDE
.idea/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Vitals provenance data
.vitals/

# Local Claude docs (non-versioned)
claudedocs/

# Local MCP config (user-specific paths)
.mcp.json

# Internal export scripts (sensitive, not for public repo)
scripts/export-devs-500plus.ts

# Batch scan data directory (runtime artifacts, gitignored entirely)
scripts/data/
scripts/geocache-preview.json
scripts/repos-*.json

# Internal notes and audit docs (not for public repo)
REPRISE.md
research-*.md

# ESLint cache
.eslintcache
.worktrees/
.claude/worktrees/

# Serena — tout ignoré
.serena/

# Claude Code local settings (machine-specific paths + hook logic)
.claude/settings.local.json
.claude/scripts/
.claude/scheduled_tasks.lock
.claude/tasks/

# Context harness (local test tooling)
.ctxharness.yml

# Cyber-defense pipeline output (analysis artifacts)
cyber-defense-*.json
cyber-defense-report.md
cyber-defense-*.md

# Analysis/audit artifacts
dep-scope-report.md
vercel-logs.txt

# One-shot repo audit pipeline (The Product Group live, not part of StarMapper)
scripts/audit-repos/

# Graphify — generated knowledge graph + AST/semantic cache
graphify-out/

# pnpm local store (when store-dir is inside the repo)
.pnpm-store/

# AI tool caches and local configs
.codex/
.code-review-graph/
.grepai/
.contributors-checkpoint.json
