services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: starmapper
      POSTGRES_USER: starmapper
      POSTGRES_PASSWORD: starmapper
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
